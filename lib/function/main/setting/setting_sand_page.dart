import 'package:flutter/material.dart';

class SettingSandPage extends StatelessWidget {
  const SettingSandPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("setting_sand_page.dart"),
    );
  }
}
