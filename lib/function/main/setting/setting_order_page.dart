import 'package:flutter/material.dart';

class SettingOrderPage extends StatelessWidget {
  const SettingOrderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("setting_order_page.dart"),
    );
  }
}
